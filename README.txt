### Steps to run the application (even though it is not complete) ###

1. install nodemon and concurrently globally
    ie. npm i -g concurrently nodemon

2. npm install in both root and client folder

3. run "npm run dev" command in the terminal to fire both the client and server concurrently.

